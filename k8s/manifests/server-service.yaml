apiVersion: v1
kind: Service
metadata:
  name: server-service
  namespace: hackathon
spec:
  type: ClusterIP
  selector:
    app: server
  ports:
    - port: 4000
      targetPort: 4000
      protocol: TCP
